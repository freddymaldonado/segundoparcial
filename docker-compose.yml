services:
  auditor:
    build: .
    container_name: guardrails-auditor
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-5.1}
    ports:
      - "8000:8000"
    volumes:
      - ./samples:/app/samples:ro
      - ./output:/app/output
